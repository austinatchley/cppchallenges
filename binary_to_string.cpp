#include <iostream>
#include <cinttypes>
#include <vector>

template <typename iter>
std::string convert(iter begin, iter end) {
    std::string out = "";
    for (T elem : v) {
        
    }
}

int main(int argc, char** argv) {
    using namespace std;

    vector<uint8_t> v = {40, 65, 20};
    cout << convert<uint8_t>(v.begin(), v.end()) << endl;
}
